## Codebase Patterns

- **envelope.ts**: ok()/fail()/emitRaw()/isJsonMode() with capture mode for testing. BLOCKING_CODES: NO_CONFIG, NO_DATABASE, SCHEMA_MISMATCH. Exit 0=success, 1=runtime error, 2=blocked prereqs.
- **tool.ts**: defineToolCommand() wraps citty defineCommand() and pushes to toolRegistry[]. extractParametersFromArgs() derives ParameterMeta from citty ArgsDef. outputFields auto-derived from outputSchema keys when present.
- **Quality gate**: zvelte-check not installed (pre-existing). biome, tsgo, svelte-check-rs, and bun test all pass. Only zvelte-check fails (exit 127).
- **Entry point split**: index.ts is thin (initConfig + runMain), main.ts exports the command tree with no side effects. Tests should import main.ts, not index.ts.
- **Monorepo structure**: packages/cli/src/ for CLI, packages/core/src/ for business logic, packages/web/src/ for frontend. CLI imports core via @fin/core.
- **tools command**: Plain defineCommand() (infrastructure). GLOBAL_FLAGS for fin: --json, --db, --format. getSortedRegistry() sorts by category then name. Text output to stderr, JSON via ok()/fail().
- **health command**: Plain defineCommand() (infrastructure). Standalone path resolution (no initConfig dependency). Uses loadConfig() from @fin/core for validation. emitRaw() for custom envelope. Exit 2 when blocked.
- **config commands**: defineToolCommand() pattern. show uses accessor functions (getGroupIds, getAllGroupMetadata, getAccountsByGroup, getFinancialConfig) from @fin/core/config. validate uses Bun.TOML.parse() + FinConfigSchema.safeParse() for structured errors. Both catch NO_CONFIG and INVALID_CONFIG. Text output to stderr.
- **Incremental migration**: When migrating command groups (e.g. view), rename legacy file to *.legacy.ts, export only unmigrated commands, and import them alongside new defineToolCommand() commands in the new directory's index.ts. This preserves all subcommands during progressive migration.

---

## Progress Log

## 2026-02-09 - ENG-001: Create JSON envelope module and tool registry
- Created packages/cli/src/envelope.ts with ok(), fail(), emitRaw(), isJsonMode(), rethrowCapture(), setCaptureMode(), setJsonModeOverride(), EnvelopeCapture
- Created packages/cli/src/tool.ts with defineToolCommand(), toolRegistry[], ToolMeta, ParameterMeta, OutputFieldSchema, OutputSchema types
- BLOCKING_CODES adapted for fin domain: NO_CONFIG, NO_DATABASE, SCHEMA_MISMATCH
- Files changed: packages/cli/src/envelope.ts (new), packages/cli/src/tool.ts (new)
- **Learnings:** Reference implementation from .tmp/let/ copies almost directly. Only BLOCKING_CODES and command prefix (fin vs let) differ. biome config references /Users/han/ which doesn't exist on this machine -- use tsgo for type checking.
---

## 2026-02-09 - ENG-002: Split entry point into index.ts and main.ts
- Created packages/cli/src/main.ts exporting root command tree via defineCommand()
- Refactored packages/cli/src/index.ts to thin wrapper: initConfig() + runMain(main)
- main.ts has no process.exit() or runMain() calls
- Verified: `fin view accounts` and `fin report cashflow --group=personal` work
- Files changed: packages/cli/src/main.ts (new), packages/cli/src/index.ts (modified)
- **Learnings:** citty setup() hook vs module-scope init are equivalent for entry points. Module-scope is simpler for index.ts since it only runs as an executable.
---

## 2026-02-09 - ENG-003: Implement tools command
- Created packages/cli/src/commands/tools/index.ts with toolsCommand using plain defineCommand()
- JSON mode: ok() with version, tools array, globalFlags; single-tool lookup; NOT_FOUND error
- Text mode: catalog grouped by category to stderr; single-tool detail to stderr
- Wired toolsCommand into main.ts subCommands
- Files changed: packages/cli/src/commands/tools/index.ts (new), packages/cli/src/main.ts (modified)
- **Learnings:** Global flags for fin differ from let (--json, --db, --format vs --json, --data-dir, --cache-dir, --config-dir, --sources-dir). New commands go in directories (tools/index.ts) while legacy ones remain as flat files (view.ts, report.ts).
---

## 2026-02-09 - ENG-004: Implement health command
- Created packages/cli/src/commands/health/index.ts with healthCommand using plain defineCommand()
- Five checks: config exists (blocking), config validates (blocking), database (info), rules file (degraded), inbox dir (info)
- Standalone path resolution (findMonorepoRoot, resolveConfigPath, resolveDbPath, resolveRulesPath, resolveInboxPath) - runs before initConfig()
- Uses loadConfig() from @fin/core for TOML+Zod validation without adding smol-toml dependency to CLI
- JSON mode: emitRaw() with custom envelope containing status/checks/summary; exit 2 when blocked
- Text mode: human-readable check list to stderr with +/x/! icons and fix commands
- Wired healthCommand into main.ts subCommands
- Files changed: packages/cli/src/commands/health/index.ts (new), packages/cli/src/main.ts (modified)
- **Learnings:** smol-toml is a core dependency, not CLI. Reuse loadConfig() from @fin/core/config for validation rather than duplicating TOML parsing. Health command needs standalone path resolution since it runs diagnostics before initConfig().
---

## 2026-02-09 - ENG-005: Implement config show and config validate commands
- Created packages/cli/src/commands/config/index.ts with configCommand group (show, validate subcommands)
- Created packages/cli/src/commands/config/show.ts using defineToolCommand() with name 'config.show'
- Created packages/cli/src/commands/config/validate.ts using defineToolCommand() with name 'config.validate'
- show returns groups, accounts (by group), financial params, configPath
- validate uses Bun.TOML.parse() + FinConfigSchema.safeParse() for structured Zod validation errors
- Both registered in tool registry and appear in fin tools --json
- Wired configCommand into main.ts subCommands
- Files changed: packages/cli/src/commands/config/ (new dir, 3 files), packages/cli/src/main.ts (modified)
- **Learnings:** smol-toml is not available in CLI package; use Bun.TOML.parse() instead. Biome enforces type imports before value imports in the same specifier. Extract helper functions to reduce cognitive complexity below 15.
---

## 2026-02-09 - ENG-006: Implement view accounts and view transactions commands
- Created packages/cli/src/commands/view/accounts.ts using defineToolCommand() with name 'view.accounts'
- Created packages/cli/src/commands/view/transactions.ts using defineToolCommand() with name 'view.transactions'
- Created packages/cli/src/commands/view/index.ts with viewCommand group wiring both new commands plus legacy ledger/balance
- Renamed packages/cli/src/commands/view.ts to view.legacy.ts (trimmed to only export ledger + balance)
- Updated main.ts to import viewCommand from new view/ directory
- Both commands appear in fin tools --json catalog with correct metadata
- JSON mode: ok() with accounts/total or transactions/count + meta.count
- Text mode: preserves existing table rendering via renderOutput()
- Files changed: packages/cli/src/commands/view/ (new dir, 3 files), packages/cli/src/commands/view.legacy.ts (renamed+trimmed), packages/cli/src/main.ts (modified)
- **Learnings:** When migrating subcommands incrementally, rename the legacy file (view.ts -> view.legacy.ts) and export only the unmigrated commands. The new directory index.ts imports both new defineToolCommand() commands and legacy commands as subcommands. This avoids breaking existing functionality while progressively migrating.
---

## 2026-02-09 - ENG-007: Implement view ledger and view balance commands
- Created packages/cli/src/commands/view/ledger.ts using defineToolCommand() with name 'view.ledger'
- Created packages/cli/src/commands/view/balance.ts using defineToolCommand() with name 'view.balance'
- Deleted packages/cli/src/commands/view.legacy.ts (all view subcommands now migrated)
- Updated view/index.ts to wire viewLedgerCommand and viewBalanceCommand directly (no legacy imports)
- view ledger: JSON returns entries array, count, total; supports --account, --from, --to, --limit
- view balance: JSON returns balance sheet fields (assets, liabilities, equity, income, expenses, netWorth, netIncome); supports --as-of
- Both registered in tool registry and appear in fin tools --json catalog
- Files changed: packages/cli/src/commands/view/ledger.ts (new), packages/cli/src/commands/view/balance.ts (new), packages/cli/src/commands/view/index.ts (modified), packages/cli/src/commands/view.legacy.ts (deleted)
- **Learnings:** Once all subcommands in a group are migrated, the legacy file can be fully deleted and the index.ts simplified to import only from the new defineToolCommand() files. Balance sheet data from getBalanceSheet() returns a flat object that maps directly to the JSON envelope without transformation.
---

## 2026-02-09 - ENG-008: Implement report cashflow command
- Created packages/cli/src/commands/report/cashflow.ts using defineToolCommand() with name 'report.cashflow'
- Created packages/cli/src/commands/report/index.ts with reportCommand group (cashflow new + legacy imports)
- Renamed packages/cli/src/commands/report.ts to report.legacy.ts (exports health, runway, reserves, categories, audit, summary)
- Updated main.ts to import reportCommand from new report/ directory
- JSON mode: ok() with series array and totals object; meta.count for series length
- Text mode: preserves existing table rendering with CASHFLOW_COLUMNS
- --expenses flag switches between getGroupMonthlyCashflowSeries() and getGroupPureMonthlyCashflowSeries()
- INVALID_GROUP error for bad --group values in JSON mode
- Files changed: packages/cli/src/commands/report/cashflow.ts (new), packages/cli/src/commands/report/index.ts (new), packages/cli/src/commands/report.legacy.ts (renamed+trimmed), packages/cli/src/main.ts (modified)
- **Learnings:** The report group migration follows the same incremental pattern as view: rename legacy file, export individual commands, import them in the new directory index.ts. buildCashflowData() helper keeps run() clean and separates data construction from output concerns.
---

## 2026-02-09 - ENG-009: Implement report health and report runway commands
- Created packages/cli/src/commands/report/health.ts using defineToolCommand() with name 'report.health'
- Created packages/cli/src/commands/report/runway.ts using defineToolCommand() with name 'report.runway'
- Health: JSON returns series + latest; supports --group (required), --from, --to
- Runway: two modes -- single group (--group) and consolidated (--consolidated --include=group1,group2)
- Consolidated mode returns series + latest + groups array in JSON envelope
- Removed health/runway from report.legacy.ts, updated report/index.ts to wire new commands
- Files changed: packages/cli/src/commands/report/health.ts (new), packages/cli/src/commands/report/runway.ts (new), packages/cli/src/commands/report/index.ts (modified), packages/cli/src/commands/report.legacy.ts (trimmed)
- **Learnings:** Biome forbids non-null assertions (noNonNullAssertion). Use `(arr[i] ?? null)` instead of `arr[i]!` for safe array access. Runway command requires careful mode branching -- extract handleConsolidatedMode/handleSingleMode helpers to keep run() under complexity limit.
---

## 2026-02-09 - ENG-010: Implement report reserves and report categories commands
- Created packages/cli/src/commands/report/reserves.ts using defineToolCommand() with name 'report.reserves'
- Created packages/cli/src/commands/report/categories.ts using defineToolCommand() with name 'report.categories'
- Reserves: JSON returns series + latest; supports --group (required), --from, --to
- Categories: flattened breakdown/median subcommands into --mode flag (default: breakdown)
- Breakdown mode: returns categories array + total; Median mode: returns categories array + estimatedMonthly
- Extracted validateGroupArg() and validateModeArg() helpers to keep run() cognitive complexity under 15
- Removed reserves and categories from report.legacy.ts; updated report/index.ts to wire new commands
- Files changed: packages/cli/src/commands/report/reserves.ts (new), packages/cli/src/commands/report/categories.ts (new), packages/cli/src/commands/report/index.ts (modified), packages/cli/src/commands/report.legacy.ts (trimmed)
- **Learnings:** When a command has multiple modes (breakdown vs median), use a --mode flag instead of subcommands for flatter agent consumption. Extract validation into typed assert functions (asserts x is T) to both reduce complexity and give TypeScript narrowing.
---

## 2026-02-09 - ENG-011: Implement report audit command
- Created packages/cli/src/commands/report/audit.ts using defineToolCommand() with name 'report.audit'
- JSON mode: ok() with payees array + total; meta.count for payee count
- Text mode: preserves existing table rendering with AUDIT_COLUMNS (payee, total, monthlyAvg, count, account, lastDate)
- --account required with INVALID_INPUT error; --group optional with INVALID_GROUP error
- Extracted buildAuditData() and typed assert guards (validateAccountArg, validateGroupArg)
- Removed audit from report.legacy.ts; updated report/index.ts to wire reportAuditCommand
- report.legacy.ts now only exports summary (last remaining legacy command)
- Files changed: packages/cli/src/commands/report/audit.ts (new), packages/cli/src/commands/report/index.ts (modified), packages/cli/src/commands/report.legacy.ts (trimmed)
- **Learnings:** Audit command is straightforward since it has a single mode with required --account and optional --group. The buildAuditData() helper pattern keeps run() clean by separating validation+query from output. After this migration, report.legacy.ts only re-exports summary -- next migration (ENG-012) will eliminate the legacy file entirely.
---

## 2026-02-09 - ENG-012: Implement report summary command (text mode)
- Migrated all 20+ helpers from packages/cli/src/commands/summary.ts to packages/cli/src/commands/report/summary.ts
- Wrapped in defineToolCommand() with name 'report.summary', registered in tool catalog
- Text mode preserves exact same markdown output (balances, snapshot, asset allocation, last month, cashflow, income/expense flows, expense tree, consolidated, balance sheet, methodology)
- JSON mode scaffolded with placeholder data (ENG-013 will flesh out structured data)
- Extracted renderSnapshotSection() from renderGroupSection() to keep cognitive complexity below biome threshold
- Deleted report.legacy.ts (summary was the last remaining legacy command in the report group)
- Updated report/index.ts to import reportSummaryCommand directly (no more legacy imports)
- Files changed: packages/cli/src/commands/report/summary.ts (new), packages/cli/src/commands/report/index.ts (modified), packages/cli/src/commands/report.legacy.ts (deleted)
- **Learnings:** The summary command is the largest single migration (464 lines, 20+ helpers). Biome requires type imports to be sorted after value imports within the same import block (e.g., `type SankeyFlowData` after `isGroupId`). Large render functions with many sequential sections need extraction into sub-functions to stay under biome's cognitive complexity limit of 15. report.legacy.ts is now fully eliminated -- all report subcommands use defineToolCommand().
---

## 2026-02-09 - ENG-013: Add JSON structured data mode to report summary
- Replaced placeholder JSON mode with buildSummaryJson() that extracts the same raw data the markdown renderers use
- buildGroupJson() per group: balances, snapshot (runway, lastMonthNet, netWorth, medianSpend), assetAllocation, lastMonth (with MoM), cashflow series, incomeSources, expenseSinks, expenseTree
- buildConsolidatedJson(): totalBalance, runway, burnRate, consolidated expenseTree across all liquid accounts
- Balance sheet data from getBalanceSheet() mapped directly to envelope
- JSON helper functions (pctNum, buildPersonalAllocation, buildReserveAllocation, etc.) parallel the markdown renderers but return structured objects
- flattenExpenseTreeJson() returns recursive tree structure preserving accountId, name, amount, children
- Text mode (markdown) remains unchanged from ENG-012
- Files changed: packages/cli/src/commands/report/summary.ts (modified)
- **Learnings:** ok() returns `never` so no `return` is needed after it -- TypeScript's unreachable code detection catches this. JSON builders should parallel markdown renderers 1:1 to ensure data parity. Extract numeric pctNum() alongside string pctOf() to serve both output modes from the same allocation logic.
---

## 2026-02-09 - ENG-014: Implement import command
- Migrated packages/cli/src/commands/import.ts to packages/cli/src/commands/import/index.ts using defineToolCommand()
- JSON mode: ok() envelope with processedFiles, skippedFiles, totalTransactions, duplicateTransactions, journalEntriesCreated, transferPairsCreated, entryErrors, accountsTouched, unmappedDescriptions, archivedFiles
- Text mode: preserves existing human-readable output (processed files, skipped files, results summary, unmapped descriptions, archived files)
- Error handling: IMPORT_ERROR code with fail() envelope in JSON mode, stderr in text mode
- Registered as non-idempotent tool (idempotent: false) since import mutates the database
- Deleted old flat import.ts file; import now resolves via import/index.ts
- Files changed: packages/cli/src/commands/import/index.ts (new), packages/cli/src/commands/import.ts (deleted), packages/cli/src/main.ts (modified)
- **Learnings:** Import is a top-level command (not nested under a group) so its tool name is just "import" not "import.index". The import command doesn't need initConfig() in its run() because main.ts index.ts already calls it in setup. Old flat files can be deleted once the directory replacement is wired in main.ts.
---

## 2026-02-09 - ENG-015: Implement sanitize discover, migrate, and recategorize commands
- Created packages/cli/src/commands/sanitize/discover.ts using defineToolCommand() with name 'sanitize.discover'
- Created packages/cli/src/commands/sanitize/migrate.ts using defineToolCommand() with name 'sanitize.migrate'
- Created packages/cli/src/commands/sanitize/recategorize.ts using defineToolCommand() with name 'sanitize.recategorize'
- Created packages/cli/src/commands/sanitize/index.ts with sanitizeCommand group (discover, migrate, recategorize)
- Discover: JSON returns descriptions array + count; supports --unmapped, --min, --account filters
- Migrate/Recategorize: --dry-run returns plan only, full run returns plan+result; both marked idempotent: false
- Migrate plan: toUpdate/alreadyClean/noMatch; Recategorize plan: toUpdate/alreadyCategorized/noMatch
- Deleted old flat sanitize.ts; updated main.ts to import sanitizeCommand from new directory
- Files changed: packages/cli/src/commands/sanitize/ (new dir, 4 files), packages/cli/src/commands/sanitize.ts (deleted), packages/cli/src/main.ts (modified)
- **Learnings:** When a flat file and directory with same name coexist (sanitize.ts vs sanitize/index.ts), TypeScript resolves to the flat file first. Must delete the flat file before the directory index.ts takes effect. Sanitize commands use loadRules() from @fin/core which handles both generic and external rules loading with caching. Migrate/recategorize share the same plan+result JSON pattern, making them consistent for agent consumption.
---

## 2026-02-09 - ENG-016: Create contract test harness and fixtures
- Created packages/cli/tests/harness.ts with in-process execution via capture mode
- Created packages/cli/tests/contract.test.ts with 5 passing tests (tools catalog, tool lookup, tool not found, health envelope, health check IDs)
- Harness: resetConfig() between runs, FIN_HOME env var for temp fixture, setCaptureMode()/setJsonModeOverride() ref-counted
- assertValidEnvelope() validates: JSON parse, ok boolean, meta.tool match, meta.elapsed >= 0, data (success) or error.code/message/hint (failure)
- Fixture: minimal TOML config with one group/account/bank, inbox dir for health checks
- Files changed: packages/cli/tests/harness.ts (new), packages/cli/tests/contract.test.ts (new)
- **Learnings:** fin harness differs from let: uses resetConfig() instead of resetPaths()/resolvePaths(), and FIN_HOME env var instead of LET_DATA_DIR/LET_CONFIG_DIR. tools command needs no config (reads in-memory registry). health command has standalone path resolution so FIN_HOME is sufficient. Bun test runner picks up tests from packages/cli/tests/ via root tsconfig include.
---

