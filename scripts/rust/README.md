# Rust Binary Install Helpers

- `verify_fin_path.sh`: verify expected runtime binary path exists and is executable
- `guard_no_legacy.sh`: fail when legacy runtime paths or patterns are reintroduced
